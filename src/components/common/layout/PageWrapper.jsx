import styled from "styled-components";
import FillParent from "./FillParent";

const PageWrapper = styled(FillParent)`
    /* overflow-y: auto; */
    overflow-x: clip;
`;

export default PageWrapper;
